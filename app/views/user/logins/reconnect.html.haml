%h1= "Reconnect login \##{params[:id]}"

= form_tag user_login_reconnection_path(params[:id]), method: :put do
  Username
  = text_field_tag :username
  %br
  Password
  = password_field_tag :password
  %br
  = submit_tag 'Reconnect'